# Iteration 01 UI Fix Report (06)

## 작성일
- 2026-03-03

## 분석 내용
- 보드 트리에서 상위/하위 드래그 이벤트 경계가 불명확해 하위 동일 레벨 이동이 막히는 케이스가 발생
- 메모 컨텍스트 메뉴가 `fixed` + 변환 컨텍스트 영향으로 마우스 포인트와 다른 위치(중앙)에 표시
- 메모 노출 순서 변경(앞/뒤/맨앞/맨뒤) 로직이 반복 실행 시 기대 순서를 보장하지 못함
- 메모 드래그 시 캔버스 스크롤/줌 좌표 반영 누락과 오프셋 보정값으로 포인터와 메모 위치가 분리
- 메모 리사이즈는 명시적 핸들이 없어 조작성이 떨어지고 저장 타이밍도 불명확
- 메모 타입 3종이 시각적으로 동일하게 보임

## 수정 파일
- `src/components/BoardTree.tsx`
- `src/components/MemoCard.tsx`
- `src/pages/MainPage.tsx`
- `src/styles/app.css`
- `docs/test/results/iteration-01-test-result.md`

## 수정 결과
- 보드 드래그 시작 지점을 `li` 전체가 아니라 `board-row`로 제한해 하위 보드 동일 레벨 위치 이동 안정화
- 메모 컨텍스트 메뉴 좌표를 메모 컴포넌트 기준 절대좌표로 변경해 우클릭 포인터 위치에 표시
- 컨텍스트 메뉴는 외부 클릭/스크롤/마우스 이탈 시 자동 닫힘 처리 보강
- 노출 순서 4개 기능을 인덱스 기반 재배열로 재작성해 반복 실행 시에도 순서 일관성 확보
- 메모 이동 좌표 계산에 캔버스 `scrollLeft/scrollTop` 반영, 드래그 오프셋 보정값 제거
- 메모 우하단 리사이즈 핸들 추가, 드래그 중 UI 크기 반영 + 마우스 업 시 API 저장으로 분리
- 메모 타입 표현을 기본(직사각형)/둥근 사각형/그림자 직사각형으로 구분되도록 스타일 보강

## 검증 결과
- `npm run build`: PASS
- `npm run test`: PASS
