# Iteration 07 Plan (WEB)

## Goal
Align implementation with architecture requirement (`Vite + React`) and provide standard `npm run dev` workflow.

## Scope
- Convert runtime entrypoint from static html serving to Vite React root entry.
- Keep existing API integration behaviors and UI ids intact for current test scripts.
- Add Vite/React dependencies and scripts (`dev`, `build`, `preview`).
- Verify with smoke/build and document outcomes.
